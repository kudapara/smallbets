<div id="<%= dom_id(message, :answering) %>" data-controller="show-if" data-show-if-role-value="expert">
  <% if message.answered? %>
    <%= button_to message_answer_path(message), method: :delete, class: "btn message__action-btn full-width",
                  data: { controller: "show-if", show_if_id_value: message.answered_by_id, action: "popup#close" } do %>
      <span>Mark unanswered</span>
    <% end %>
    <span class="btn message__action-btn full-width txt-subtle" data-controller="hide-if" data-hide-if-id-value="<%= message.answered_by_id %>">
      Answered by peer
    </span>
  <% else %>
    <%= button_to message_answer_path(message), method: :post, data: { action: "popup#close" }, class: "btn message__action-btn full-width" do %>
      <span>Mark answered</span>
    <% end %>
  <% end %>
</div>