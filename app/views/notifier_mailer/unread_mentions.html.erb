<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <style>
        p {
            font-family: sans-serif;
        }
    </style>
  </head>
  <body>
    <p>Hi <%= @user.name %>,</p>
    <p>You have <%= pluralize(@messages.size, "unread mention") %>:</p>
    
    <% @messages.each do |message| %>
      <p>
        <%= message.creator.name %> on <%= message.created_at.strftime("%b %-d, %-I:%M %p") %> in <%= link_to message.room.name, room_at_message_url(message.room, message) %>:
        <br/>
        <i><%= message.plain_text_body %></i>
      </p>
    <% end %>
    <p>
      <br/>
      <small>
        --
        <br/>
        To opt out of these emails, click <%= link_to "here", mailkick_unsubscribe_url(@user, "notifications") %>.
      </small>
    </p>
  </body>
</html>
